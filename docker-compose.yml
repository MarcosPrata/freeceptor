version: "3.9"

services:
  mongo:
    build:
      context: .
      dockerfile: Dockerfile.mongo
    container_name: freeceptor-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
