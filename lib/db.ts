/**
 * Cliente de banco de dados.
 * Troque por Prisma, Drizzle, etc. quando for persistir dados.
 *
 * Exemplo com Prisma:
 *   import { PrismaClient } from "@prisma/client";
 *   const prisma = new PrismaClient();
 *   export { prisma };
 */

// Placeholder: sem DB por enquanto
export const db = null;
