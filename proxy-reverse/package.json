{
  "name": "freeceptor-proxy-reverse",
  "version": "0.3.0",
  "description": "Reverse proxy agent for Freeceptor - connects via WebSocket and exposes local services",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "sh -c 'set -a; [ -f .env ] && source .env; set +a; node --import tsx/esm --watch src/index.ts'",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.0",
    "typescript": "^5"
  }
}
